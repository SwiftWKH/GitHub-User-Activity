# GitHub-User-Activity
In this project,  a simple command line interface (CLI) is built to fetch the recent activity of a GitHub user and display it in the terminal. This project will help hone programming skills, including working with APIs, handling JSON data, and building a simple CLI application.

To create a file
'' | Out-File filename

To del
del filename